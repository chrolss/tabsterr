services:
  tabsterr:
    build: .
    container_name: tabsterr
    ports:
      - "8082:3000"
    volumes:
      - ./tabs:/app/tabs
      - ./backing_tracks:/app/backing_tracks
    environment:
      - PORT=3000
    restart: unless-stopped
